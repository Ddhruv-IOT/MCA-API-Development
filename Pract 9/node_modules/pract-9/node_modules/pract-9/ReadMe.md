# Perfrom Pagination. Implement an API that returns a paginated list of users from a sample dataset

## README Guide:

Use endpoints:
- GET /users?page=1&limit=5 -> Fetch paginated users (default limit: 10)
